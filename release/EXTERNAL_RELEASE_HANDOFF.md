# Clasp external release handoff

Observed: **2026-08-25**
Status: **local product installed; corrected signed Store build uploaded, processed `VALID`, and attached to version 1.0.0; private Developer ID-notarized and Sparkle-signed GitHub draft independently verified; Bundle ID, aligned App Store Connect record, Apple/Sparkle credentials, public GitHub repository, protected `main`, and protected release environments exist; remaining listing/legal gates, signed-sandbox acceptance, App Review submission, direct publication, and post-publication update acceptance remain fail-closed**

This guide starts where the verified local build and repository release scaffolding stop. It does not authorize accepting agreements, choosing legal/compliance answers, generating credentials, publishing source, uploading binaries, or submitting for review without Robert at the relevant gate.

## Live Apple state

- **App Store Connect → Apps** contains the confirmed macOS record `Clasp: Private Markdown Notes`, Apple app ID `6804786714`, version `1.0.0`, in `Prepare for Submission`. Processed build `1.0.0 (8)` is attached and saved. Sign-in is not required and release is manual. Apple rejected the globally occupied bare product name `Clasp`; Robert approved the distinct Store name while the installed app remains `Clasp`. The permanent SKU was verified in the authenticated account and is intentionally withheld from this public repository.
- **Apple Developer → Identifiers** lists `Clasp` / `com.robertmoore.personalnotepad` under Paper LLC team `R3Z7H2TRCB`; this identifier was registered on 2026-08-24 with no optional capability selected. Apple's default In-App Purchase capability remains present.
- **Apple Developer → Certificates** now lists valid Mac App Distribution and Mac Installer Distribution certificates for Paper LLC through 2027-08-24, in addition to the pre-existing Developer ID and development rows. Their independently generated private keys are present in the login Keychain and match the imported public certificates. The selected direct identity is the Developer ID Application certificate with SHA-256 `322807574BAAB0C6030832DD5BA786F6F7FA6A84090D14523DFE3706C9D81E10`, expiring 2031-08-10; its password-protected P12 was independently reopened and fingerprint-matched before being stored in protected GitHub custody. The older same-label Developer ID certificate remains untouched. No certificate was revoked or replaced.
- **Apple Developer → Profiles** now lists `Clasp Mac App Store 2026`, an App Store profile for `R3Z7H2TRCB.com.robertmoore.personalnotepad` through 2027-08-24. Local validation bound its team, identifier prefix, bundle ID, entitlements, platform, expiry, and embedded application certificate to the exact Keychain identity.
- **App Store Connect → Business** shows the Free Apps Agreement active. EU trader compliance is incomplete. The Paid Apps Agreement is new and unsigned; do not accept it unless the product will offer paid apps or in-app purchases and Robert approves the legal action.
- **GitHub** has the approved public repository `https://github.com/RobertBMoore/clasp` with no license file. The local checkout uses it as `origin`. Pull request #10 repaired Store payload permissions, passed required CI run `32789879116`, and squash-merged as `ab09cdaceac9d2c4dbf9e762e531daf6db40e4b8`. Pull request #11 repaired fail-closed direct-release verification for macOS 26, passed required CI run `32792418524`, and squash-merged as `f62c246153469e2d4fcd97d13a342db8a81aaad8`. Earlier repository-protection publication is recorded by PR #4 / run `32774974087` / merge `09033781b131fad5812034132cb50d3214f436d1`. Untracked `artifacts/` data was not staged or published.
- **GitHub branch protection** applies to `main`: pull requests are required, approvals are deliberately not required for the solo maintainer, branches must be current, `test-and-package-local` from GitHub Actions must pass, conversations must be resolved, and linear history is required. Force pushes and deletions remain disabled.
- **GitHub release environments** `release` and `app-store-release` require reviewer `RobertBMoore`, disallow administrator bypass, allow self-review for the solo maintainer, and accept deployments only from protected branches. The direct environment has all four required variables and six protected secrets: the public update-feed URL, dedicated Sparkle public key, exact Developer ID identity and certificate pin, Developer ID P12/password, dedicated Sparkle private key, and Apple notarization P8/key ID/issuer. All four distinct `CLASP_PUBLISHED_*` repository mirrors are configured for tag-triggered acceptance. Protected run `32792893098` proved the runtime custody by notarizing/stapling, generating a signed appcast, independently verifying the finished set, creating a private draft, and removing imported credentials. The Store environment has the protection acknowledgement, team/application-prefix assertions, accepted-build assertion `8`, exact application/installer identities and SHA-256 fingerprints, live Privacy URL, application P12/password, installer P12/password, and exact Clasp provisioning profile.
- **Public support/security** now has Robert's approved Paper LLC publisher identity and monitored `hello@paper.ai` contact. GitHub Issues and private vulnerability reporting are enabled. HTTPS-enforced public GitHub Pages build `1172738403` published the canonical Support and Privacy sources from merge commit `09033781b131fad5812034132cb50d3214f436d1`; anonymous HTTPS requests to both canonical URLs returned `200` on 2026-08-24. Live email-intake and end-to-end private-report submission testing remain pending.
- **Apple-processed Store build** came from protected run `https://github.com/RobertBMoore/clasp/actions/runs/32790427285` and exact protected `main` commit `ab09cdaceac9d2c4dbf9e762e531daf6db40e4b8`. `Clasp-1.0.0-8-AppStore.pkg` has SHA-256 `09dcc9361d7a95c5cb699507b7a5d381c3d925b88130c346aae2e3fe6d80ccb0` and GitHub artifact digest `sha256:5795d154c7675bf5f22fa490632b88641ad8e3a1307bc375ba1d31411dd2381e`; exact application/installer pins, provisioning profile, App Sandbox entitlements, migration and Privacy manifests, PackageKit policy, checksum, packaged-payload parity, root-readable/traversable payload permissions, absence of Sparkle/Accessibility capture, `ITSAppUsesNonExemptEncryption=false`, artifact upload, and credential cleanup passed in protected CI and independent downloaded-artifact validation. After the superseded package exposed and drove repair of `ITMS-90255`, `xcrun altool` ContentDelivery `26.40.1 (174001)` validated and uploaded the corrected package with no errors. Apple delivery UUID `2d7fac89-473a-46a2-818c-c7cd530ef0b0` reached terminal `VALID` / `APP_STORE_ELIGIBLE`, confirmed non-exempt encryption false, and build `1.0.0 (8)` was attached and saved to the manual-release version. This is upload/processing evidence, not App Review acceptance.
- **Private direct-release draft** run `https://github.com/RobertBMoore/clasp/actions/runs/32792893098` passed from exact protected `main` commit and `v1.0.0` target `f62c246153469e2d4fcd97d13a342db8a81aaad8`. Apple notarization submission `47657903-19c4-4eb5-9cbd-a3dbb9c990ac` was accepted, stapling and Gatekeeper passed, the Sparkle-signed appcast passed the fail-closed offline verifier, and imported credentials were removed. The downloaded private draft independently passed archive-only verification with notarization required. `Clasp-1.0.0-8.zip` SHA-256 is `eed9f67ae2f1bdb83881910ec1084157bf894e1867dfb4cc7556987710075d18`; the appcast SHA-256 is `c7a835d6f193a2b881bc8b324546457b3342fc8d555d77eae17889e80844655f`. This is draft evidence only; no direct release is public and no post-publication update acceptance has run.

The unauthenticated Sparkle feed requires this public repository. Preserve the existing Apple/Sparkle keys and do not rotate, export, or replace them implicitly. Do not accept legal agreements, choose trader/content-rights/age-rating/App-Privacy answers, publish the direct draft, or submit the app for review without action-time confirmation.

## Required order of operations

1. **Preserve the existing App Store Connect record** for Apple app ID `6804786714` and the exact registered Bundle ID. Revalidate its version/build state immediately before packaging or upload; keep the permanent SKU in authenticated account custody rather than public source.
2. **Preserve and operationalize the required Apple identities and profile** after their approved 2026-08-24 creation:
   - For direct distribution, preserve and use only the selected Developer ID Application certificate pin `322807574BAAB0C6030832DD5BA786F6F7FA6A84090D14523DFE3706C9D81E10`; keep the older same-label identity untouched.
   - Preserve the Mac App Distribution and Mac Installer Distribution private keys outside source control; do not revoke or replace them implicitly.
   - Preserve the exact Clasp App Store profile outside source control and rotate it deliberately before expiry.
   - Preserve the completed protected CI exports and GitHub secrets; never expose their bytes or passwords in logs or source.
3. **Resolve the remaining Account Holder gates**: EU trader status, tax/banking if applicable, age rating, content rights, and App Privacy. The Free Apps Agreement is active, and Robert confirmed the Store channel's operating-system-only encryption declaration as exempt on 2026-08-24.
4. **Preserve the published HTTPS pages** for Support and Privacy Policy from `main/docs`. Anonymous reachability is proven; test the monitored email/private-report intake routes before entering the URLs in App Store Connect.
5. **Maintain the protected GitHub boundary** already configured at `https://github.com/RobertBMoore/clasp`: every follow-up must use a pull request, pass `test-and-package-local`, remain current with `main`, resolve conversations, and preserve linear history. Never publish untracked `artifacts/` data or credentials. The repository intentionally has no license until Robert chooses one.
6. **Preserve the completed protected GitHub environments and repository-variable mirrors** from `release/README.md`. Required reviewers, protected-branch-only deployment, direct feed and Sparkle trust root, selected direct certificate pin, notarization API values, live Store Privacy URL, Store account assertions, exact identity labels, and signing/profile secrets are configured and runtime-proven by the private draft. Never paste private keys or credential bytes into source, issues, logs, screenshots, or this handoff; rotate only through a separately approved custody procedure.
7. **Preserve exact Store-package custody after Apple processing.** The selected build is bound in `AppStore/SUBMISSION_HANDOFF.md` to protected run `32790427285`, package SHA-256 `09dcc936…ccb0`, and Apple delivery UUID `2d7fac89-473a-46a2-818c-c7cd530ef0b0`. Later merge `f62c246…a81aaad8` is direct-verifier-only and does not change this Store artifact. Any Store source or binary declaration change requires a higher build number, regeneration, revalidation, upload, and rebinding.
8. **Preserve the processed build selection** in App Store Connect. Chrome confirmed build `1.0.0 (8)` attached and saved; do not remove, replace, or submit it implicitly.
9. **Run signed-sandbox acceptance** on a disposable environment with synthetic data, then complete `AppStore/MIGRATION_ACCEPTANCE.md`.
10. **Compare the screenshot candidates** in `AppStore/Screenshots/` with the exact signed Store build, recapture any drift, validate them, and have Robert approve their ordering and copy.
11. **Submit only after every readiness and custody field is complete** and Robert gives final human approval at the review action.

## Direct-update publication order

1. Preserve the configured `main` protection and `release` environment gates in the approved public GitHub repository.
2. Preserve the selected Developer ID certificate, exact `322807…D81E10` fingerprint, protected export, and custodian; do not create, revoke, rotate, or substitute a certificate implicitly.
3. Preserve the completed separate Sparkle EdDSA keypair; never reuse an Apple certificate key or expose the private key.
4. Review the private signed/notarized baseline `v1.0.0` draft from run `32792893098` and its independently verified archive/checksum/appcast set.
5. Publish the reviewed baseline manually only after exact action-time approval; publication is intentionally not implied by draft success.
6. Require the post-publication acceptance workflow to pass. A real N-to-N+1 Sparkle update must later pass `DIRECT_UPDATE_ACCEPTANCE.md` on a clean Mac before claiming end-to-end in-app update acceptance.

Until the remaining listing/legal, Store signed-sandbox acceptance, App Review submission, direct publication, and post-publication update gates above are complete, `/Applications/Clasp.app` remains the correct local installation. Apple has processed and attached the separately signed Store build, and the private GitHub draft remains the current direct-distribution candidate; neither is yet public Store availability evidence.
